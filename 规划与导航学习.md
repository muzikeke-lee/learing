# 规划与导航学习

> 分册 **6 / 9** ｜ 上级 [学习文档](学习文档.md) ｜ 术语 [术语表](术语表.md)
> 前置：[定位与重定位学习](定位与重定位学习.md)、[建图与点云学习](建图与点云学习.md) ｜ 最近更新 2026-09-07

---

## 0. 这册学什么

**从「一张点云」到「狗真的走过去了」**：全局粗路径 → 局部避碰 → 航点闭环执行。
这册的技术含量最高，也是我自研成分最重的一层。

学完应该能：说清 NP-MFS 那个「统一可通行性图」为什么能取代一整套分层+楼梯检测方案。

---

## 1. 项目与文件

### 1.1 全局规划：NP-MFS（当前主线）

代码主体 `3DMap_NP-MFS/dimos-m20/`（dimos 的 fork）。

| 路径 | 是什么 |
|---|---|
| `3DMap_NP-MFS/memory/QUICKSTART.md` | 🔴 **先读这篇**，工作记忆 + 快速上手 |
| `docs/design/m20_traversability_graph_plan.md` | 🔴 **设计文档，第 1–9 节是完整演进记录（必读）** |
| `dimos/robot/deeprobotics/m20/traversal/` | **核心包**：`params.py` / `trav_grid.py` / `trav_graph.py` / `planner.py` |
| `tools/trav_plan_rerun.py` | 规划 + Rerun 可视化 |
| `tools/trav_plan_cli.py` | 纯规划，出 waypoint |
| `tools/trav_grid_rerun.py` | 栅格可视化 |
| `tools/trav_connectivity.py` | **连通性 / snap 诊断** |
| `dimos/robot/deeprobotics/m20/tests/test_traversal_smoke.py` | 冒烟测试，**当前 11/11** |
| `3DMap_NP-MFS/map_pcd/full_cloud2.pcd` | 主测试地图（三层，z ∈ [-11.9, 7.7]） |

### 1.2 局部执行：M20 自研包

| 路径 | 是什么 |
|---|---|
| `m20/path_planner.py` | **A\* 占据栅格规划**（2D 局部） |
| `m20/patrol_behaviors.py` | 导航 / 巡逻编排：**闭环到点 + 站起再走** |
| `m20/safety_monitor.py` | 雷达近障急停 → [分册 3](激光雷达学习.md) |
| `m20/waypoint_registry.py` | 航点 → [分册 5](建图与点云学习.md) |
| `dimosmain/tools/planner_viz` | 规划可视化诊断 |

### 1.3 D1 导航

| 路径 | 是什么 | 坑 |
|---|---|---|
| `D1sidefellow/d1_docs/导航bringup与绕障.md` | 起导航栈 / 调参 / 绕障 / 狗不动 | 🔴 **改绕障前必读那两条红线** |
| `D1sidefellow/config/d1_env.sh.example` | 调参旋钮逐个含义 | 含**被否 / 未实现方案的墓碑** |
| `D1sidefellow/`（`feature/d1-side-follow`） | 穿门 nav + **UWB 侧跟随**全栈，狗上实际部署 | — |
| `agibot-d1/dimos-m20/`（`feat/agibot-d1`） | 穿门 nav 线 | — |
| `agibot-d1/nav-api/` | 导航 API | 待确认 |
| `agibot-d1/推箱子修复规格_20260721.md` | 「推箱子」问题的修复规格 | 待确认是什么现象 |

### 1.4 外部参考

| 路径 | 是什么 |
|---|---|
| `scanPlanner/SCAN-Planner/` | **SCAN-Planner**：Spatial Collision-Aware Local Planning for Route-Guided Long-Range Quadruped Navigation（Han Zheng 等，arXiv [2606.19555](https://arxiv.org/abs/2606.19555)）。定位为「给上层任务（自主探索、VLN）提供稳健的底层规划基础」。2026-07-09 放出主算法，2026-07-13 社区贡献 ROS2 接口（`ros2-community` 分支） |
| `3DMap_NP-MFS/reference/far_planner` | 外部：FAR Planner |
| `3DMap_NP-MFS/reference/staircase_autonomy` | 外部：楼梯自主 |

---

## 2. 核心机制

### 2.1 ★ NP-MFS：统一可通行性图

**问题**：M20（大型四足 + Livox Mid-360）要在**多楼层**建筑里做全局粗路径规划。

**核心思想**：把整栋多层点云化成**一张统一的可通行性图**。

> **楼梯不单独检测，它只是「连接不同高度的渐变可通行边」。**

⇒ **一次 A\* 天然处理跨层**。输出粗 waypoint，交 M20 自带的局部层执行。

### 2.2 为什么这个抽象是关键胜利

早期方案是：**分层 → 楼梯检测 → 可见图 → floor_graph 拼接**（四个阶段，已被替代）。

| 早期方案 | 统一图方案 |
|---|---|
| 楼梯是**特例**，要专门检测 | 楼梯是**普通的边**，只是高度有渐变 |
| 每层各建一张图，再拼接 | 一张图 |
| 跨层要专门的 floor_graph 逻辑 | A\* 自己就跨了 |
| 楼梯检测失败 ⇒ 整栋不通 | 没有「楼梯检测」这个失败点 |

⇒ **教训**：当某个东西需要「特殊检测 + 特殊处理 + 特殊拼接」时，先问「能不能把它表达成通用结构的一个普通情形」。
消掉一个特例，同时消掉了它的检测失败模式、它的拼接逻辑、和它的参数。

设计文档 `m20_traversability_graph_plan.md` **第 1–9 节是完整演进记录** —— 这是本册最有价值的阅读材料，
因为它记录的是「为什么走了弯路、怎么想通的」，而不只是最终结果。

### 2.3 跑一次仿真

```bash
cd /home/liancheng.li/3DMap_NP-MFS/dimos-m20
.venv/bin/python -u tools/trav_plan_rerun.py ../map_pcd/full_cloud2.pcd \
  --start 19.42 -34.99 -11.92 --goal 9.42 -19.24 -0.42 \
  --serve --host <你的开发机IP>
```

start 和 goal 的 z 分别是 **-11.92** 和 **-0.42** ⇒ 这条测试用例本身就是**跨层**的（差约 11.5m）。

### 2.4 分层规划：全局粗 + 局部精

| 层 | 谁做 | 输出 |
|---|---|---|
| 全局粗路径 | **我的** NP-MFS（跨层 A\*） | 粗 waypoint 序列 |
| 局部避碰执行 | **M20 厂商自带**的局部层 | 实际速度指令 |
| 近障急停 | **我的** `safety_monitor`（雷达 UDP） | 急停 |

⇒ 自研只做全局层和安全层，**不重写局部层** —— 这是有意的分工，减少和厂商运控抢方向盘。

### 2.5 patrol_behaviors 的「闭环到点 + 站起再走」

两个细节暗示了四足特有的问题：

- **闭环到点**：不是发一次目标就完事，要持续检查是否真到了（四足打滑 / 被挡，开环必偏）
- **站起再走**：四足在趴/坐姿态下不能直接走 ⇒ 导航动作要先确保姿态正确（联动 [分册 2](DIMOS学习.md) 的 `gait_daemon`）

---

## 3. 🔴 红线

| 红线 | 说明 |
|---|---|
| **改 D1 绕障前必读 `导航bringup与绕障.md` 那两条红线** | 有个**被否方案留下的证据全是正面的**，极易误判成「该恢复」 |
| **`d1_env.sh.example` 里有被否方案的墓碑** | 动绕障参数前先读，别重走死路 |
| **玻璃区禁止自主导航** | 雷达 + 地图双盲 → [分册 3](激光雷达学习.md) |
| **别互相整树覆盖 D1 两条分支的共享文件** | 会回退侧跟随 → [分册 1](平台与部署学习.md) |
| **规划的输入是地图，地图换了路径就全错** | → [分册 5](建图与点云学习.md) |

### 关于「证据全是正面的被否方案」

这条值得单独想清楚：一个方案被否了，但留下的**证据看起来全是支持它的**。
可能的成因：那些正面证据是在**特定条件下**取得的，而否掉它的原因是另一个条件下的失败（可能没留下同样醒目的记录）。

⇒ **看到「证据全是正面却被否」时，要找的不是更多正面证据，而是当初否它的那个失败场景。**
这就是为什么墓碑要写「为什么否」而不只是「已否」。

---

## 4. 动手清单

- [ ] 精读 `m20_traversability_graph_plan.md` 第 1–9 节，把演进过程画成时间线贴进笔记
- [ ] 跑通 2.3 那条命令，在浏览器里看到跨层路径
- [ ] 跑 `tools/trav_connectivity.py` 做一次连通性 / snap 诊断，理解 snap 在解决什么问题
- [ ] 跑 `test_traversal_smoke.py`，确认还是 11/11
- [ ] 读 `traversal/params.py`，把每个参数的物理含义写进笔记（这是理解可通行性判据的最快路径）
- [ ] 读 `trav_grid.py` → `trav_graph.py` → `planner.py` 三步的数据流，画出来
- [ ] 读 `path_planner.py`，对比它的 2D A\* 和 NP-MFS 的图 A\* 差别
- [ ] 读 `导航bringup与绕障.md` 那两条红线，用自己的话写出「为什么不能恢复那个方案」
- [ ] 通读 `d1_env.sh.example` 的墓碑段，列出所有被否方案

---

## 5. 自测问题

- [ ] NP-MFS 的核心思想一句话是什么？
- [ ] 为什么**不单独检测楼梯**？「渐变可通行边」这个抽象同时消掉了哪几样东西？
- [ ] 早期「分层 → 楼梯检测 → 可见图 → floor_graph 拼接」为什么被替代？它有哪个致命失败点？
- [ ] 从这个替换里能提炼出什么通用的设计教训？
- [ ] `traversal/` 四个文件（`params` / `trav_grid` / `trav_graph` / `planner`）的数据流是什么？
- [ ] 全局层、局部层、安全层分别由谁负责？为什么**不重写**局部层？
- [ ] `patrol_behaviors.py` 为什么要「闭环到点」而不是发一次目标？为什么要「站起再走」？
- [ ] 改 D1 绕障前必读的那两条红线是什么？
- [ ] 为什么那个被否方案的证据「全是正面的」却不能信？看到这种情况应该去找什么？
- [ ] SCAN-Planner 和我自己的 `path_planner.py` + `safety_monitor.py` 组合，差别在哪？有什么可搬的？

---

## 6. 笔记（日期倒序）

### 模板（复制这段往下写）

```markdown
### 2026-XX-XX ｜ <主题>
- **在学**：<项目/文件路径>
- **搞懂了**：<一句话结论，重点写「从代码里看不出来的」>
- **踩的坑**：<现象 → 真实原因 → 判据>
- **代码位置**：`path/to/file.py:123`
- **已回写到**：<哪份权威文档；没回写就写「待回写」>
- **仍不懂**：<搬到第 7 节>
```

<!-- 从这里往下写笔记 -->

---

## 7. 本册疑问

- [ ] NP-MFS 的输出 waypoint 实际接进 M20 局部层了吗，还是只到仿真？
- [ ] `traversal/` 只在 `3DMap_NP-MFS/dimos-m20` 那棵树里 —— 什么时候合回主线？
- [ ] D1 有没有对应的多楼层方案，还是只做单层？
- [ ] `nav-api/` 是什么？和 `patrol_behaviors.py` 什么关系？
- [ ] `推箱子修复规格_20260721.md` 里「推箱子」是什么现象（狗把障碍推着走？）
- [ ] UWB 侧跟随和 waypoint 导航是两套独立逻辑还是共用一套底层？
- [ ] `far_planner` / `staircase_autonomy` 评估结论是什么？为什么最终自研？
- [ ] SCAN-Planner 的 `ros2-community` 分支值得试吗？

# 语音交互学习

> 分册 **8 / 9** ｜ 上级 [学习文档](学习文档.md) ｜ 术语 [术语表](术语表.md)
> 前置：[DIMOS学习](DIMOS学习.md) ｜ 最近更新 2026-09-07

---

## 0. 这册学什么

**从人说话到狗动腿**的完整链路：唤醒 → ASR → 意图分流（知识库 / 闲聊 / 动作）→ TTS / 动作执行。

学完应该能：画出这条链路的时序图，说清每一跳跑在**哪台主机**、走**什么协议**。

> 这册的特点是**跨主机 + 跨进程**最多的一层 —— 也因此最需要先学完 [分册 1](平台与部署学习.md) 和 [分册 2](DIMOS学习.md)。

---

## 1. 项目与文件

### 1.1 M20 语音链

| 路径 | 跑在哪 | 是什么 |
|---|---|---|
| `dimosmain/runtime/voice_frontend_104.py` | **104 (GOS)** | 豆包 ASR/TTS 前端。USB 麦 + Jabra 音箱**插在 104 上** ⇒ 前端就在 104。⚠️ 待装 `doubao_speech` |
| `dimosmain/runtime/voice_io_runner.py` | **106 (NOS)** | 命令服务，TCP 端口 `M20_VOICE_SERVER_PORT`（9700） |
| `dimosmain/dimos/robot/deeprobotics/m20/voice/blueprint_b` | 106，dimos 内 | 语音 blueprint |
| `.../voice/qwen_agent` | 106，dimos 内 | Agent（意图理解 / 对话） |
| `.../voice/wake_word` | 106，dimos 内 | 唤醒词 |
| `dimosmain/m20-voice/` | — | 语音相关资产 / 文档 |

### 1.2 D1 语音链

| 路径 | 是什么 |
|---|---|
| `agibot-d1/doubaoyuying/` | 豆包语音前端。分支 **`D1-voice`**，入口 **`kb_realtime.py`** |
| `agibot-d1/doubaoyuying/CLAUDE.md` | 🔴 **权威文档** |
| `agibot-d1/doubaoyuying_d1.zip` | 打包版（待确认是部署包还是备份） |

**D1 链路**：ASR → **KB / 闲聊**分流 → TTS；**动作命令转发狗上 9700**。

### 1.3 两台狗的对照

| | **M20** | **D1 Max** |
|---|---|---|
| ASR/TTS | 豆包 | 豆包 |
| 前端在哪 | **独立主机 104**（音频硬件在那） | Orin NX 上（`doubaoyuying`） |
| 意图理解 | `voice/qwen_agent`（dimos 内） | KB / 闲聊分流（`kb_realtime.py`） |
| 唤醒 | `voice/wake_word` | 待确认 |
| 动作命令去哪 | `voice_io_runner`（9700 TCP，106） | **转发狗上 9700** |
| 知识库 | 待确认 | **有 KB** |

> **共同点：动作命令都落到 9700。** 这个端口是「语音世界」和「运动世界」的**唯一接口** ——
> 记住这一个数字，两台狗的语音链都能接上。

---

## 2. 核心机制

### 2.1 为什么前端和命令服务要分开

M20 上前端在 104、命令服务在 106，中间走网络。看起来绕，但**硬约束是音频硬件的位置**：
USB 麦和 Jabra 音箱插在 104 上，音频采集必须在 104 做。

⇒ 得到一个通用的部署原则：**IO 型进程绑硬件所在主机，计算/控制型进程绑主计算主机。**
（对照 [分册 2](DIMOS学习.md) 2.3 的「module 还是独立进程」判断标准 —— 这是同一个原则的第四条。）

### 2.2 意图分流：三条出路

一句话进来，出路有三种，分流点是本层的核心逻辑：

| 出路 | 去哪 | 例子 |
|---|---|---|
| **知识库（KB）** | 查 KB → TTS 播报 | 「这栋楼几层？」 |
| **闲聊** | LLM → TTS 播报 | 「你好」 |
| **动作命令** | **转发 9700** → 运动执行 | 「往前走」「回充」 |

⇒ 分流错了的后果不对称：把动作当闲聊 = 狗不动（能发现）；**把闲聊当动作 = 狗乱动（危险）**。
所以分流应该偏保守 —— 这一点值得在代码里验证是不是这么设计的（见第 4 节）。

### 2.3 `voice_io_runner` 为什么是独立进程

对外提供 **TCP 服务**，要独立于 blueprint 起停 ⇒ 不能做成 dimos module
（详见 [分册 2](DIMOS学习.md) 2.3）。

这也意味着：**语音挂了不影响导航，导航重启不影响语音** —— 进程边界就是故障边界。

### 2.4 姿态类语音命令的落点

「趴下 / 站起 / 调姿态」这类命令最终落到 `gait_daemon.py`（DDS 直发 `/GAIT`，~0.5s 热切换），
而不是走厂商接口 —— 详见 [分册 2](DIMOS学习.md) 2.4。

⇒ 语音链的末端有两个执行器：**运动类** → 速度环 / 导航；**姿态类** → `gait_daemon`。

---

## 3. 红线速查

| 红线 | 说明 |
|---|---|
| **动作命令都走 9700** | 改端口要同时改两边 |
| **M20 前端必须在 104** | 音频硬件在那，不能挪到 106 |
| **`doubao_speech` 在 104 上待装** | 装之前 M20 语音链跑不通 |
| **把闲聊误判成动作 = 狗乱动** | 分流要保守 |

---

## 4. 动手清单

- [ ] 追一条动作命令的全链路（麦 → ASR → 分流 → 9700 → 执行），画时序图贴进笔记，标出每跳的主机和协议
- [ ] 追一条 KB 问句的全链路，对比两条链在哪里分叉
- [ ] 找到分流的判断代码，确认它对「疑似动作命令」是保守还是激进
- [ ] 用 `nc` 或 `tools/udp_test` 直接往 9700 发一条命令，绕开语音验证下游可用（⚠️ 空旷场地、有人盯）
- [ ] 在 104 上查 `doubao_speech` 装没装；没装的话记录阻塞点
- [ ] 读 `kb_realtime.py`，写清 KB 的数据来源和更新方式
- [ ] 对比 M20 的 `qwen_agent` 和 D1 的 `kb_realtime.py`，列出两套意图理解的差别

---

## 5. 自测问题

- [ ] M20 语音链每一跳跑在哪台主机、走什么协议？为什么前端必须在 104？
- [ ] 从这个部署约束能提炼出什么通用原则？
- [ ] D1 的链路是什么（ASR → ? → ?）？入口文件是哪个？分支是哪个？
- [ ] 意图分流有哪三条出路？分流错误的后果为什么**不对称**？应该偏保守还是激进？
- [ ] 动作命令为什么要转发到 9700 而不是本地执行？
- [ ] `voice_io_runner` 为什么是独立进程而不是 dimos module？这带来什么可靠性好处？
- [ ] 「趴下 / 站起」这类姿态命令最终落到哪？为什么不走厂商接口？
- [ ] 语音链末端的两个执行器分别是什么？

---

## 6. 笔记（日期倒序）

### 模板（复制这段往下写）

```markdown
### 2026-XX-XX ｜ <主题>
- **在学**：<项目/文件路径>
- **搞懂了**：<一句话结论，重点写「从代码里看不出来的」>
- **踩的坑**：<现象 → 真实原因 → 判据>
- **代码位置**：`path/to/file.py:123`
- **已回写到**：<哪份权威文档；没回写就写「待回写」>
- **仍不懂**：<搬到第 7 节>
```

<!-- 从这里往下写笔记 -->

---

## 7. 本册疑问

- [ ] M20 的 `doubao_speech` 为什么还没装？缺什么（依赖 / 授权 / 网络）？
- [ ] M20 有知识库吗？如果没有，`qwen_agent` 靠什么回答建筑相关问题？
- [ ] D1 的唤醒词方案是什么？和 M20 的 `wake_word` 是同一套吗？
- [ ] 两台狗的语音层能抽多少公共部分？（对照 [学习文档](学习文档.md) 第 5 节的跨册疑问）
- [ ] 9700 是明文 TCP 吗？在现场网络里有没有安全考虑？
- [ ] 豆包 ASR/TTS 是走云端 API 吗？断网时语音链降级成什么？
- [ ] `doubaoyuying_d1.zip` 是部署包还是历史备份？和 `doubaoyuying/` 差多少？
- [ ] 语音说「回充」和遥控器按「去充电」是同一条链吗？（对照 [分册 3](激光雷达学习.md) 2.5 的 ARC 排查）

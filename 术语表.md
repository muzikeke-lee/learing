# 术语表

> 上级 [学习文档](学习文档.md) ｜ 九本分册共用 ｜ 最近更新 2026-09-07
>
> 收录规则：**只收我自己项目里实际出现的术语**，通用教科书概念不收。
> 标 ⚠️ 的是**带坑的术语** —— 望文生义会出错。

---

## 平台与部署（[分册 1](平台与部署学习.md)）

| 术语 | 含义 |
|---|---|
| **M20** | 云深处（DeepRobotics）四足机器人，大型，配 Livox Mid-360。三台主机 |
| **D1 Max** | 智元四足机器人，中文名**「追影」**。两块计算板（Orin NX + RK3588） |
| **中狗 ZSM-1** | 第三台机器狗，已有接入 dimos 的记录 |
| **AOS / GOS / NOS** | M20 三台主机的角色：厂商运控（103）/ 语音前端（104）/ 命令执行（106） |
| **Orin NX** | D1 主计算板（NVIDIA Jetson） |
| **RK3588** | D1 运动控制板（瑞芯微）。⚠️ **在独立内网段，必须经 NX 跳板** |
| ⚠️ **MagicDNS** | Tailscale 的域名服务。**不能当抗变写法** —— 试过，两天就失效 |
| **governor** | Linux CPU 频率调度策略。`performance` 对 D1 是**零损失纯赚** |
| ⚠️ **git 失真** | 狗上 HEAD 落后 258 commit ⇒ `git status` 的 ` M`/`??` 全部不可信。判孤本只能 md5 对拍 |
| **md5 对拍** | 用 md5 比对开发机与狗上的同名文件，判断是否真的有差异。git 不可信时的唯一手段 |
| **merge-base** | 两个分支的共同祖先。D1 两条线的 merge-base = `84be476`，⚠️ **别互相整树覆盖共享文件** |
| **孤本** | 只存在于某一处（通常是狗上）、没有进 git 的文件版本 |
| ⚠️ **墓碑** | 文档里刻意保留的「被否方案 + 为什么否」记录。防止后人「恢复」已被证伪的方案 |

---

## DIMOS 框架（[分册 2](DIMOS学习.md)）

| 术语 | 含义 |
|---|---|
| **DIMOS** | 机器人栈框架（上游 [dimensionalOS/dimos](https://github.com/dimensionalOS/dimos)，本地基线 **v0.0.11**，vendored） |
| **blueprint** | 描述「哪些 module 怎么连」的装配声明。**读代码先读 blueprint 再读 module** |
| **module** | blueprint 里的一个节点，纯数据流处理单元 |
| **autoconnect** | 按 blueprint 声明自动接线的机制 |
| **可选 peer** | ★我们打的补丁：`Module \| None` 类型的 peer 可以缺省。**升级 dimos 时第一个要重打的补丁** |
| **overlay** | 叠在厂商/上游代码之上的自研补丁层（`drdds_overlay/`、各 robot 包） |
| **sidecar** | 与主进程并跑、只干一件转发/桥接的小进程（如 `m20_lidar_sidecar.py`） |
| **vendored** | 把依赖的源码直接放进仓库（而不是 pip 装），便于改 |
| **DDS** | 机器人常用中间件。厂商没给接口时，**DDS 直发可以绕过** |
| ⚠️ **zenoh** | D1 厂商 ROS 栈用的中间件。**查询必带 zenoh 环境**，否则 `ros2 topic/service` 全空、极易误判「没起」 |
| **SixAxisStand** | M20 的姿态调节步态，GaitParam **`0xF001` = 61441**。厂商只绑遥控器，DDS 直发 `/GAIT` 可复现 |
| **9700** | 动作命令服务端口（TCP）。**两台狗通用** —— 语音世界和运动世界的唯一接口 |
| **7777** | M20 雷达 sidecar → `safety_monitor` 的 UDP 端口 |
| **8082** | D1 的 NX ↔ RK 板间 SDK UDP 端口。**两块板之间只有这一条通道** |

---

## 传感器（[分册 3](激光雷达学习.md)）

| 术语 | 含义 |
|---|---|
| **Livox Mid-360** | M20 的激光雷达，IMU 内置 |
| ⚠️ **`rslidar_head`** | D1 的雷达 frame_id。**IMU 和点云共用这个名字，但坐标轴差 90°** —— 填单位阵必发散 |
| ⚠️ **acc 单位 g** | D1 三路 IMU 的加速度单位是 **g 不是 m/s²**。差 9.8 倍，滤波必崩 |
| **外参** | 传感器之间的相对位姿。⚠️ **D1 的雷达外参不用自己标**，厂商已给 |
| **USS** | Ultrasonic Sensor，超声传感器。D1 有，权威文档 `RADARA22test/README.md` |
| **RTK** | 载波相位差分 GNSS，厘米级定位。`D1RTK/` |
| **UWB** | Ultra-Wideband，超宽带测距。D1 侧跟随靠它跟人 |
| ⚠️ **ARC** | D1 自动回充对准流程。**不需要地图/定位** —— 排查时别往 fit%/重定位那边查 |
| **hostapd** | Linux AP 服务。「去充电」不出现且 RK `dmesg` 有 `no role for macid` ⇒ 在 RK 上重启它（**给桩断电没用**） |
| ⚠️ **玻璃双盲区** | 激光穿透玻璃 ⇒ 雷达看不到、地图上也没有。**唯一兜底是人工标注 + 现场盯防** |

---

## 定位（[分册 4](定位与重定位学习.md)）

| 术语 | 含义 |
|---|---|
| ⚠️ **fit%** | D1 定位置信度。**< 90% 录航点会静默偏 0.7m** |
| ⚠️ **假收敛** | 定位看起来收敛（`/load_map_service` 回 `success=True`）但实际错位。**接口返回值不可信** |
| **校正器** | D1 定位链里的校正环节，权威文档 `定位与校正器.md` |
| **重定位** | 给定位一个初始位姿。M20：UDP `Type=2101 Cmd=1` `{PosX,PosY,PosZ,Yaw}`。**开机/丢失必做**，是录航点和导航的前提 |
| **`Type=1007 Cmd=2`** | M20 读导航位姿（地图系 `PosX,PosY,Yaw`） |
| ⚠️ **Yaw 约定** | M20：**弧度**、**0 = +x = 东**、**逆时针为正**。已现场校验（偏差 2°）。换平台必须重新校验 |
| **LIO** | Lidar-Inertial Odometry，激光惯性里程计（FAST-LIO 系） |
| ⚠️ **先验图** | 自建 LIO **必须**拿厂商 `map.pcd` 当先验，否则航点 / pgm / 巡线脚本**全废** |
| **EuRoC** | VIO/SLAM 标准评测数据集，`datasets/euroc/` |
| **ICP** | Iterative Closest Point，点云配准算法。`GitHubs/localization1/icp_localization_ros2` |

---

## 地图与点云（[分册 5](建图与点云学习.md)）

| 术语 | 含义 |
|---|---|
| **`.pgm` + `.yaml`** | 2D 占据栅格图 + 元数据（`resolution` / `origin`）。**成对使用**，光有 pgm 无法定位坐标 |
| **`.pcd`** | 点云文件格式 |
| **`.rrd`** | **Rerun** 录制格式，可存档回放 |
| **Rerun** | 可视化工具（本地 0.29.2）。`--serve --host <ip>` 可以「本机跑、别处看」 |
| **航点 waypoint** | 导航目标点。⚠️ **D1 存在 SQLite 不是 JSON**；⚠️ **webviz 不读 DB**，界面上看到的不是真相 |
| **webviz** | D1 的地图/航点可视化界面。⚠️ 不读 DB |
| ⚠️ **地图 = 坐标基准** | 地图不只是定位输入，还是航点 / pgm / 巡线脚本共享的参照系。**换图 = 换掉所有下游资产的坐标系** |
| **楼板 slab** | `pcd_floor_split` 检出的楼层分界面，输出 `slab_z` |
| **相对突起度 prominence** | `scipy.find_peaks` 的判据：峰比周围高多少。⚠️ **替代固定阈值** —— 全局阈值会被地面强峰压制掉楼层峰 |
| **Cloud2BIM** | 被蒸馏的上游项目（MIT），点云→IFC/BIM 管线 |

---

## 规划与导航（[分册 6](规划与导航学习.md)）

| 术语 | 含义 |
|---|---|
| **NP-MFS** | 我的多楼层规划方案代号：统一可通行性图 + 跨层单次 A\* |
| ⚠️ **可通行性图 traversability graph** | 把点云化成「能走 / 不能走 + 代价」的图。**楼梯不单独检测**，只是连接不同高度的渐变可通行边 |
| **floor_graph** | 早期方案里的分层图拼接结构，**已被统一图取代** |
| **snap** | 把起点/终点吸附到图上最近可通行节点。`trav_connectivity.py` 诊断的对象之一 |
| **粗 waypoint** | 全局层输出、交局部层执行的稀疏路径点 |
| **闭环到点** | 持续检查是否真到达，而不是发一次目标就完事。四足打滑/被挡时开环必偏 |
| **站起再走** | 导航前先确保姿态正确（四足趴/坐时不能直接走） |
| **穿门 nav** | D1 的一条导航能力线（`feat/agibot-d1`） |
| **侧跟随 side follow** | D1 的 UWB 跟人能力（`feature/d1-side-follow`，**狗上实际部署**） |
| **SCAN-Planner** | 外部工作：四足长距导航的 spatial collision-aware 局部规划器（arXiv 2606.19555） |
| **VLN** | Vision-Language Navigation，视觉语言导航 |

---

## 视觉检测（[分册 7](视觉检测学习.md)）

| 术语 | 含义 |
|---|---|
| **YOLO26-s** | 基线检测模型，`weights/yolo26s.pt`。实测 91 FPS @ imgsz=960（5070 Ti，未量化） |
| **ultralytics** | YOLO 的实现库，本地 8.4.120 |
| **TACO** | 垃圾检测公开数据集，`ground/` 的冷启动数据 |
| **jsai** | `pavement/` 的数据来源（2000 张） |
| ⚠️ **sm_120 / Blackwell** | RTX 50 系的计算能力。**必须 cu128+ 的 PyTorch 轮子**；PyPI 默认源可能不含 sm_120 kernel，判据是训练时报 `no kernel image is available for execution on the device` |
| ⚠️ **pavement ≠ sidewalk** | 前者车载视角沥青马路裂缝，后者机器人视角铺装砖破损。**视角/类别/模型都不通用**，迁移价值仅限「什么是裂缝」这层纹理先验 |

---

## 语音（[分册 8](语音交互学习.md)）

| 术语 | 含义 |
|---|---|
| **豆包** | 字节的 ASR/TTS 服务，两台狗都在用 |
| **`doubao_speech`** | 豆包语音的 Python 包。⚠️ M20 的 104 上**待装** |
| **ASR / TTS** | 语音识别 / 语音合成 |
| **KB** | Knowledge Base，知识库。D1 语音的一条分流出路 |
| **意图分流** | 一句话进来分成 KB / 闲聊 / 动作命令三条出路。⚠️ **把闲聊误判成动作 = 狗乱动**，分流要保守 |
| **wake_word** | 唤醒词模块 |
| **qwen_agent** | M20 语音里的 agent 模块 |
| **`kb_realtime.py`** | D1 语音前端入口（分支 `D1-voice`） |

---

## 科研（[分册 9](科研与专利学习.md)）

| 术语 | 含义 |
|---|---|
| **IRS** | Intelligent Reflecting Surface，智能反射面 |
| **IFDM** | ⚠️ 全称待确认 |
| **NOMA** | Non-Orthogonal Multiple Access，非正交多址接入 |
| **和速率最大化** | sum-rate maximization |
| **蒸馏** | 从开源项目里只取核心算法、砍掉重依赖（如 `pcd_floor_split` 之于 Cloud2BIM） |

---

## 贯穿全套的三条元认知

这三条在多本分册里反复出现，值得单独记住：

| 教训 | 出处 |
|---|---|
| 🔴 **「成功」不代表可用** —— 接口返回值、pip 安装、进程起来了，都要用独立判据复核末端状态 | 假收敛（[4](定位与重定位学习.md)）、Blackwell kernel（[7](视觉检测学习.md)）、避障链静默失效（[3](激光雷达学习.md)） |
| 🔴 **静默失效比报错危险** —— 会报错的坑不叫坑，真正的坑都是「照样能跑但结果是错的」 | fit<90% 偏 0.7m（[5](建图与点云学习.md)）、改错 `agibot/d1` vs `deeprobotics/m20`（[2](DIMOS学习.md)）、避障链断（[3](激光雷达学习.md)） |
| 🔴 **消掉特例胜过处理特例** —— 需要「特殊检测 + 特殊处理 + 特殊拼接」的东西，先想能不能表达成通用结构的普通情形 | 楼梯即渐变边（[6](规划与导航学习.md)）、相对突起度替代全局阈值（[5](建图与点云学习.md)） |

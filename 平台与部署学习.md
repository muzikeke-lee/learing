# 平台与部署学习

> 分册 **1 / 9** ｜ 上级 [学习文档](学习文档.md) ｜ 术语 [术语表](术语表.md)
> 前置：无（**从这册开始**） ｜ 最近更新 2026-09-07

---

## 0. 这册学什么

**连上狗、看清谁在跑什么、把代码安全地弄上去。** 这一册不涉及任何算法，但它是所有其他分册的前提 ——
后面每一册的「动手」都要先能登上机器人。

学完应该能做到：闭着眼画出两台狗的主机 / 网段 / 进程图，并说清每台主机上**哪个进程不能碰**。

🔴 **凭据不在本仓库**。SSH / sudo 密码、跳板具体写法、完整内网 IP 见本机
`agibot-d1/CLAUDE.md`「连狗」节 和 `dimosmain/README.md`「机器人拓扑」节。本册主机用末位代号指代。

---

## 1. 项目与文件

| 路径 | 是什么 | 坑在哪 |
|---|---|---|
| `dimosmain/README.md`「机器人拓扑」 | M20 三主机分工表 | **103 不要重启** —— 有急停风险 |
| `agibot-d1/CLAUDE.md`「连狗」/「两块计算板」 | D1 登录方式、双板分工 | Tailscale IP 和节点名都会变，节点名变得比 IP 还勤 |
| `D1sidefellow/d1_docs/狗上部署与git失真.md` | 狗上 git 状态为什么不可信 | 判孤本只能 md5 对拍 |
| `D1sidefellow/d1_docs/CPU负载与调优.md` | load 高的真实来源 | 算力已拉满，`performance` governor 是零损失纯赚 |
| `dimosmain/deploy/deploy.py` | M20 一键整栈部署（经跳板同步 `dimos/` + `runtime/` + `config/`） | — |
| `dimosmain/tools/side_follow_debug/` | Side Follow 快速部署：run + overlay **两路 md5 校验** + env 配置层校验 + 重启后自动 arm | 比整栈部署快，日常改 side-follow 用这个 |
| `D1sidefellow/runtime/D1脚本介绍.md` | 狗上各脚本干什么 | — |
| `agibot-d1/d1_*.sh` | 本机侧运维脚本：`d1_env.sh` `d1_reloc.sh` `d1_switch_map.sh` `d1_reload_map_wp.sh` `d1_loc_watchdog.sh` `d1_loc_recover.sh` | 看名字能猜出用途，但**参数含义要读脚本** |
| `agibot-d1/d1_logs_*/` `~/d1_test_logs/` | 现场测试日志留档 | — |

### 两台狗的主机对照

| | **M20**（云深处） | **D1 Max**（智元「追影」） |
|---|---|---|
| 计算板数 | 3 台主机 | 2 块板 |
| 厂商运控 | **103 (AOS)**：`basic_server` + `rl_deploy`，UDP 30000。**不要重启** | **RK3588**：运动控制，内网独立网段 |
| 主计算 | **106 (NOS)**：dimos blueprint + `voice_io_runner` + `gait_daemon` + 雷达 sidecar + `lidar_imu_bridge` | **Orin NX**：主计算，Tailscale 可达 |
| 语音前端 | **104 (GOS)**：USB 麦 + Jabra 音箱，豆包 ASR/TTS | 跑在 Orin NX 上（`doubaoyuying`） |
| 板间通信 | UDP 30000（运控） | **只走 SDK UDP 8082**，NX↔RK 无 ssh 凭据 |
| 开发机可直连 | 104，106 经 104 跳板 | Orin NX；RK 必须经 NX 跳板 |
| 地址稳定性 | 固定内网 IP | ⚠️ **Tailscale IP 和节点名都漂**（见下） |

---

## 2. 核心机制

### 2.1 M20 的三主机为什么这么分

- **103 = 厂商黑盒**。运控进程在这里，重启 = 急停风险。自研代码**一行都不部署到 103**，只通过 UDP 协议跟它说话。
- **106 = 我们的地盘**。dimos 整栈跑在这里，随便重启。
- **104 = 音频硬件在哪，前端就在哪**（USB 麦 + Jabra 音箱插在 104 上），同时兼任 106 的跳板。

这个划分的本质是：**把不可重启的厂商件隔离在一台主机上**，自研层全部集中在可重启的主机。

### 2.2 D1 的地址漂移与抗变写法

现象：Tailscale 节点名比 IP 变得还勤（观察到 `.11`→`.5`→`.9`）。

- ❌ 硬记节点名 —— 会变
- ❌ 用 MagicDNS 名当「抗变写法」 —— **试过，两天就失效**
- ✅ 开工先查当前 active 的那个；offline 的是历史残留，**还占着好名字**（最容易误连）
- ⚠️ 换 IP 后免密可能失效（`authorized_keys` 被重置）→ 需要重新把公钥追加回去

### 2.3 狗上 git 为什么全是假的

狗上 HEAD 落后 **258 个 commit** ⇒ `git status` 的 ` M`（已修改）和 `??`（未跟踪）**全部失真**：

- 一个文件显示 ` M`，可能只是因为狗上 HEAD 太老，内容其实和开发机一致
- 一个文件显示 `??`，可能在新 commit 里早就有了

⇒ **判「这个文件是不是孤本」只能 md5 对拍**，不能看 git。这也是 side_follow 部署脚本要做「run + overlay 两路 md5 校验」的原因。

### 2.4 分支与 merge-base 红线

`D1sidefellow`（`feature/d1-side-follow`，狗上实际部署）与 `dimos-m20`（`feat/agibot-d1`，穿门 nav 线）
**merge-base = `84be476`**。

🔴 **别互相整树覆盖共享文件** —— 会回退侧跟随功能。两条线各改各的，共享文件要逐个 diff。

---

## 3. 红线与已知坑

| 红线 | 后果 | 判据 / 兜底 |
|---|---|---|
| **M20 103 不要重启** | 急停风险 | 需要动运控只能走 UDP 协议 |
| **查 D1 厂商 ROS 栈必带 zenoh 环境** | `ros2 topic/service` 全空，**极易误判成「进程没起」** | 带上环境再查一遍再下结论 |
| **别互相整树覆盖 D1 两条分支的共享文件** | 回退侧跟随 | merge-base `84be476`，逐文件 diff |
| **狗上 `git status` 不可信** | 误删孤本 / 误判已同步 | md5 对拍 |
| **刷机会抹掉 `/ota/calibration_results.yaml`**（连整个 home 一起） | 标定丢失 | 拿 `config/calibration_results.restored.yaml` 补回去 |
| **`agibot-d1/` 本身不是 git 仓库** | `PROGRESS.md` 无法 commit、只在本机 | 可版本化的内容要写进子项目 |

### CPU 负载的正确认知

load 高**不是**因为某个进程写得烂，而是**调度开销 + 中断分布** —— 算力本身已经拉满。
⇒ 优化方向是调度和中断亲和性，不是砍功能。`performance` governor 是**零损失纯赚**，先开。

---

## 4. 动手清单

- [ ] 分别登上 M20 的 104 / 106 和 D1 的 Orin NX / RK3588，各跑一次 `ps` 看进程，和第 1 节的表核对
- [ ] 画出两台狗的主机 / 网段 / 进程图，贴进第 6 节笔记
- [ ] 在 D1 上带 zenoh 环境和不带各查一次 `ros2 topic list`，亲眼看到「全空」这个假象
- [ ] 在狗上找一个 `git status` 显示 ` M` 的文件，md5 对拍开发机的同名文件，验证是真改还是失真
- [ ] 读一遍 `deploy/deploy.py`，说清它同步了哪三个目录、经过谁跳板
- [ ] 读 `D1脚本介绍.md`，把 `agibot-d1/d1_*.sh` 六个脚本各写一句话进笔记

---

## 5. 自测问题

- [ ] M20 三台主机（103 / 104 / 106）各跑什么？为什么 **103 不能重启**？
- [ ] 为什么语音前端放在 104 而不是主计算 106 上？
- [ ] D1 两块板（Orin NX / RK3588）怎么分工？开发机为什么连不到 RK 所在网段，怎么绕？
- [ ] NX 和 RK 之间只走什么通道？这意味着调试时有什么限制？
- [ ] D1 的 Tailscale 节点名为什么不能硬记？为什么 MagicDNS 名**也**不算抗变写法？
- [ ] offline 的历史节点为什么危险？
- [ ] 狗上 HEAD 落后 258 个 commit ⇒ `git status` 的 ` M`/`??` 为什么全部失真？判孤本只能怎么办？
- [ ] `D1sidefellow` 和 `dimos-m20` 的 merge-base 是 `84be476` —— 为什么不能互相整树覆盖共享文件？
- [ ] side-follow 快速部署脚本做的「run + overlay 两路 md5 校验」是在防什么？
- [ ] D1 的 load 高来自什么？为什么砍功能不是正确方向？
- [ ] 查厂商 ROS 栈忘带 zenoh 环境会看到什么现象？为什么这个现象特别危险？

---

## 6. 笔记（日期倒序）

### 模板（复制这段往下写）

```markdown
### 2026-XX-XX ｜ <主题>
- **在学**：<项目/文件路径>
- **搞懂了**：<一句话结论，重点写「从代码里看不出来的」>
- **踩的坑**：<现象 → 真实原因 → 判据>
- **代码位置**：`path/to/file.py:123`
- **已回写到**：<哪份权威文档；没回写就写「待回写」>
- **仍不懂**：<搬到第 7 节>
```

<!-- 从这里往下写笔记 -->

---

## 7. 本册疑问

- [ ] M20 的 104 既是语音前端又是跳板，语音负载会不会影响跳板可靠性？
- [ ] D1 换 IP 后 `authorized_keys` 被重置 —— 是刷机行为还是 Tailscale 行为？能不能根治？
- [ ] 五份 dimos 副本的部署路径分别指向哪台机器？（和 [DIMOS学习](DIMOS学习.md) 第 3 节一起查）
- [ ] `agibot-d1/dog_deployed_b2_20260722/` 是某次部署的快照吗？和当前狗上状态差多少？
- [ ] `odin1_firmware_update_pack_*.zip` / `odin_map_view/` 属于哪条线？

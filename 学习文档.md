# 学习文档

> 用途：系统学习 `/home/liancheng.li` 下**我自己开发的东西**。
> 创建：2026-09-07 ｜ 最近更新：2026-09-07

---

## 0. 这份文档怎么用

**定位**：这里是**索引 + 学习笔记**，不是技术细节的权威出处。

| 内容类型 | 写哪里 |
|---|---|
| 可版本化的技术细节、踩坑 | 回写到各项目自己的 `README.md` / `d1_docs/*.md`（**能进 git、能给别人看**） |
| 进度、决策理由、待办 | 各项目的 `PROGRESS.md` |
| **「我学到了什么」「我还不懂什么」** | **本文档第 4、6 节** |

规则三条：

1. **第 1 节只放指针**，一句话 + 路径，细节一律不抄进来（抄了就会和源文档不同步）。
2. 第 4 节笔记**日期倒序**，用第 4.0 的模板；重点记「从代码里看不出来的东西」。
3. 第 6 节的疑问，搞懂了就搬到第 4 节并划掉，别留在原地。

---

## 1. 全局地图：我开发了什么

### 1.1 两台四足机器人（主线）

| 目录 | 是什么 | 权威文档 | 状态 |
|---|---|---|---|
| `agibot-d1/` | 智元 **D1 Max**（中文名「追影」）工作区。狗上实际部署 = `D1sidefellow/`（穿门 nav + UWB 侧跟随全栈） | `agibot-d1/CLAUDE.md` → 里面的表格再分派到 `D1sidefellow/d1_docs/*.md` | 活跃 |
| `dimosmain/` | 云深处 **M20** 语音控制 + 自主导航栈。底座 dimos v0.0.11 + 自研 M20 包 | `dimosmain/README.md` | 活跃 |
| `agibot-d1/t01/` | 又一份 dimos 树（`t01` 平台线），配套 venv `dimos-t01-venv/` | 待确认 | 待确认 |
| `dimostest/` | dimos 树的另一份副本，比 `dimosmain` 少 `bridge/` `config/` `m20_docs/` | 待确认（是早期实验树？） | 待确认 |

⚠️ **同一套 dimos 在磁盘上有 4 份**（`dimosmain` / `dimostest` / `agibot-d1/dimos-m20` / `3DMap_NP-MFS/dimos-m20` / `agibot-d1/t01`）。
学之前**先确认在读哪一份**，别在 A 树里找 B 树的改动 —— 这是最容易白花一小时的地方。

### 1.2 定位 / 建图 / 规划

| 目录 | 是什么 | 权威文档 |
|---|---|---|
| `3DMap_NP-MFS/` | **多楼层全局粗路径规划**（M20 + Livox Mid-360）。把整栋点云化成一张统一可通行性图，楼梯不单独检测，一次 A\* 跨层 | `3DMap_NP-MFS/memory/QUICKSTART.md`（先读这篇）+ `dimos-m20/docs/design/m20_traversability_graph_plan.md` 第 1–9 节演进记录 |
| `pcd_floor_split/` | 点云按楼层自动分层（Z 轴高度直方图 + `scipy.find_peaks`）。算法蒸馏自开源 Cloud2BIM | `pcd_floor_split/README.md` |
| `scanPlanner/SCAN-Planner/` | **外部**论文代码（Zheng 等，arXiv 2606.19555），四足长距导航的局部避碰规划器。参考/对标用 | 上游 `README.md` + 本地 `CLAUDE.md` |
| `3DMap_NP-MFS/reference/` | 外部参考实现：`far_planner`、`staircase_autonomy` | 各自上游 |
| `GitHubs/` | 外部定位仓库：`lidar_localization_ros2`、`ORB_SLAM3`、`ascent`、`localization1/icp_localization_ros2` + 自写 `icp_pose_bridge.py` | 各自上游 |
| `2Dmap/` `3DMap/` | 地图数据资产（`.pgm`/`.yaml` 栅格图、`.pcd` 点云、`.rrd` Rerun 录制） | 无（数据目录） |
| `datasets/euroc/` | EuRoC 数据集（VIO/SLAM 标准评测集） | 无 |

### 1.3 感知

| 目录 | 是什么 | 权威文档 |
|---|---|---|
| `recognize/` | 三个**独立**的目标检测任务共享一套环境：`ground/` 地面垃圾、`pavement/` 车行道病害、`sidewalk/` 人行道铺装病害。基线 YOLO26-s | `recognize/README.md` |

### 1.4 语音与交互

| 目录 | 是什么 | 权威文档 |
|---|---|---|
| `agibot-d1/doubaoyuying/` | D1 豆包语音前端（ASR → KB/闲聊 → TTS），入口 `kb_realtime.py`，动作命令转发狗上 9700 | `doubaoyuying/CLAUDE.md` |
| `dimosmain/m20-voice/` + `runtime/` | M20 语音链：`voice_frontend_104.py`（豆包 ASR/TTS）→ `voice_io_runner.py`（9700 TCP 命令服务） | `dimosmain/README.md` |

### 1.5 科研 / 其他

| 目录 | 是什么 |
|---|---|
| `patents/` | 专利：IRS 辅助 IFDM-NOMA 系统和速率最大化的设计方法（含 LaTeX 源） |
| `papers/` | 论文 PDF（含 2606.19555 = SCAN-Planner 那篇） |
| `logo_recolor/` | Logo 金属质感换色小实验 |
| `d1_test_logs/` `agibot-d1/d1_logs_*/` | 现场测试日志留档 |

---

## 2. 知识树：按层次看这些项目在哪一层

```
应用层     侧跟随 / 穿门导航 / 巡逻 / 语音问答 / 病害巡检
             ↑ D1sidefellow, patrol_behaviors.py, doubaoyuying, recognize
─────────────────────────────────────────────────────────────
编排层     DIMOS blueprint（module 拼装 + autoconnect）
             ↑ dimos/core/blueprints.py（★M20 打了 `Module | None` 可选 peer 的补丁）
─────────────────────────────────────────────────────────────
规划层     全局粗路径（跨层 A*）→ 局部避碰 → 航点闭环
             ↑ 3DMap_NP-MFS/traversal, path_planner.py, SCAN-Planner(参考)
─────────────────────────────────────────────────────────────
定位建图   重定位 / fit% 校正器 / 自建 LIO 研究 / 楼层分层
             ↑ D1 定位与校正器.md, D1-FAST-LIO, pcd_floor_split, GitHubs/*
─────────────────────────────────────────────────────────────
感知安全   雷达近障急停 / 超声 / RTK / 视觉检测
             ↑ safety_monitor.py + m20_lidar_sidecar.py, RADARA22test, D1RTK, recognize
─────────────────────────────────────────────────────────────
硬件接口   UDP 运控协议 / DDS 直发步态 / RTSP 取图 / 关节电池温度
             ↑ connection.py, gait_daemon.py, camera.py, 硬件与传感器.md
─────────────────────────────────────────────────────────────
基础设施   多主机拓扑 / SSH 跳板 / 一键部署 / md5 对拍
             ↑ deploy/deploy.py, tools/side_follow_debug/, 狗上部署与git失真.md
```

**同一层在两台狗上的对应关系**是最值得先建立的认知 —— 学 M20 的某一层时，顺手问「D1 这层是怎么做的、为什么不一样」。

🔴 D1 运行时用 `agibot/d1/*` **不是** `deeprobotics/m20/*`。改错文件 = 部署了不生效。

---

## 3. 建议学习路线

每阶段格式：**目标 → 读什么 → 动手验证 → 自测问题**。自测答不上来就说明还没学会，别往下走。

### 阶段 1｜平台与拓扑（能连上、知道谁在跑什么）

- **读**：`dimosmain/README.md` 的「机器人拓扑」；`agibot-d1/CLAUDE.md` 的「连狗」
- **动手**：分别画出 M20 和 D1 的主机 / 网段 / 进程图（写进第 4 节笔记）
- **自测**
  - [ ] M20 三台主机（103 AOS / 104 GOS / 106 NOS）各跑什么？为什么 103 **不能重启**？
  - [ ] D1 两块计算板（Orin NX / RK3588）怎么分工？开发机为什么连不到 168 网段？
  - [ ] D1 的 Tailscale 节点名为什么不能硬记、MagicDNS 名为什么也不算「抗变写法」？

### 阶段 2｜DIMOS 框架本体

- **读**：`dimosmain/dimos/core/blueprints.py`；`dimosmain/dimos/robot/deeprobotics/m20/` 各模块；对照 `agibot/d1/`
- **动手**：给一个已有 blueprint 画出 module 连接图，标出哪些 peer 是可选的
- **自测**
  - [ ] blueprint 的 autoconnect 怎么把 module 接起来？`Module | None` 那个补丁解决的是什么问题？
  - [ ] `runtime/` 下的进程为什么是**独立进程**而不是 dimos 里的 module？

### 阶段 3｜定位与重定位（**最容易踩坑的一层，值得多花时间**）

- **读**：`D1sidefellow/d1_docs/定位与校正器.md`（必读）；`D1sidefellow/D1-FAST-LIO/README.md`（方案研究，零代码）
- **动手**：在 D1 上把「loc 死活」的 4 种形态各判一次，验证常规 3 条判据如何全体假阴性
- **自测**
  - [ ] 为什么 `/load_map_service` 返回 `success=True` **也不能信**？什么叫假收敛？
  - [ ] 录航点前为什么必须先确认 fit ≥ 90%？不确认会静默偏多少？
  - [ ] 自建 LIO 为什么**必须**拿厂商 `map.pcd` 当先验图，自己建图会废掉什么？
  - [ ] 三路 IMU 的 acc 单位是什么？IMU 和点云 `frame_id` 同名却差 90° —— 填单位阵会怎样？

### 阶段 4｜规划与导航

- **读**：`3DMap_NP-MFS/memory/QUICKSTART.md` → `docs/design/m20_traversability_graph_plan.md`（第 1–9 节演进记录）；`m20/path_planner.py`、`patrol_behaviors.py`；`D1sidefellow/d1_docs/导航bringup与绕障.md`
- **动手**：跑 `tools/trav_plan_rerun.py` 在 `full_cloud2.pcd`（三层，z ∈ [-11.9, 7.7]）上出一条跨层路径并可视化
- **自测**
  - [ ] NP-MFS 为什么**不单独检测楼梯**？「渐变可通行边」这个抽象省掉了哪一整套旧方案？
  - [ ] 早期「分层 → 楼梯检测 → 可见图 → floor_graph 拼接」为什么被替代？
  - [ ] 改 D1 绕障前必读的那**两条红线**是什么？为什么那个被否方案的证据「全是正面的」却不能信？

### 阶段 5｜感知与安全

- **读**：`recognize/README.md`；`pcd_floor_split/README.md`；M20 `safety_monitor.py` + `runtime/m20_lidar_sidecar.py`；`D1sidefellow/RADARA22test/README.md`（超声）
- **动手**：跑一次 `split_pcd_to_floors()`，把检出的 `slab_z` 和 `2Dmap/` 里的楼层对上
- **自测**
  - [ ] 避障数据链完整路径是什么（lidar_imu_bridge → ? → ?）？
  - [ ] 为什么**玻璃是雷达 + 地图双盲区**，工程上怎么兜底？
  - [ ] `pavement/` 和 `sidewalk/` 名字接近，为什么模型不通用？车道数据的迁移价值**只**剩哪一层？
  - [ ] RTX 50 系（Blackwell, sm_120）为什么必须 cu128 以上的轮子？装错报什么错？
  - [ ] `pcd_floor_split` 为什么把原版固定阈值（0.6× 峰值）改成 `find_peaks` 相对突起度？

### 阶段 6｜语音链路

- **读**：`doubaoyuying/CLAUDE.md`（入口 `kb_realtime.py`）；`dimosmain/runtime/voice_io_runner.py`
- **动手**：追一条语音命令从麦克风到关节动作的全链路，画时序图
- **自测**
  - [ ] ASR → KB/闲聊 → TTS 的分流判断在哪做？动作命令为什么要转发到 9700 而不是本地执行？
  - [ ] M20 的姿态调节为什么要靠 DDS 直发 `/GAIT`（`SixAxisStand`，GaitParam `0xF001`=61441），而不是走厂商接口？

### 阶段 7｜部署与工程纪律

- **读**：`D1sidefellow/d1_docs/狗上部署与git失真.md`；`dimosmain/deploy/deploy.py`；`tools/side_follow_debug/`
- **自测**
  - [ ] 狗上 HEAD 落后 258 个 commit ⇒ `git status` 的 ` M`/`??` 为什么**全部失真**？判孤本只能怎么办？
  - [ ] `D1sidefellow` 和 `dimos-m20` merge-base = `84be476` —— 为什么**不能互相整树覆盖**共享文件？
  - [ ] side-follow 快速部署脚本做的「run + overlay 两路 md5 校验」是在防什么？

### 阶段 8｜科研线（可与上面并行）

- **读**：`patents/` IRS 辅助 IFDM-NOMA 和速率最大化；`papers/2606.19555v1.pdf`（SCAN-Planner）
- **自测**：SCAN-Planner 的 spatial collision-aware 局部规划，和我自己 `path_planner.py` + `safety_monitor.py` 的组合，差别在哪？有什么可搬的？

---

## 4. 学习笔记（日期倒序）

### 4.0 模板（复制这段往下写）

```markdown
### 2026-XX-XX ｜ <主题>
- **在学**：<项目/文件路径>
- **搞懂了**：<一句话结论。重点写「从代码里看不出来的」>
- **踩的坑**：<现象 → 真实原因 → 判据>
- **代码位置**：`path/to/file.py:123`
- **已回写到**：<哪份权威文档；没回写就写「待回写」>
- **仍不懂**：<搬到第 6 节>
```

### 2026-09-07 ｜ 建立学习文档
- **在学**：全局，摸清 `/home/liancheng.li` 下有什么
- **搞懂了**：项目分五块 —— 两台四足主线（D1 / M20）、定位建图规划、感知、语音、科研。真正的知识密度在各项目的 `d1_docs/*.md` 和 `docs/design/*.md`，不在代码里。
- **踩的坑**：dimos 在磁盘上有 5 份副本，读代码前必须先确认在哪一棵树。
- **仍不懂**：见第 6 节

---

## 5. 术语表

| 术语 | 含义 |
|---|---|
| **DIMOS** | 机器人栈框架（上游 `dimensionalOS/dimos`，本地基线 v0.0.11）。核心抽象是 blueprint + module + autoconnect |
| **blueprint** | DIMOS 里描述「哪些 module 怎么连」的装配声明 |
| **AOS / GOS / NOS** | M20 三台主机的角色：厂商运控 / 语音前端 / 命令执行 |
| **fit%** | D1 定位置信度指标。**< 90% 录航点会静默偏移**，是定位一层最重要的一个数 |
| **假收敛** | 定位看起来收敛（接口回 `success=True`）但实际错位的状态；常规判据会假阴性 |
| **重定位** | 给定位一个初始位姿（M20：UDP `Type=2101 Cmd=1`）。开机/丢失必做，是录航点和导航的前提 |
| **航点 waypoint** | 导航目标点序列。⚠️ D1 存在 **SQLite** 里不是 JSON，webviz 不读 DB |
| **NP-MFS** | 我的多楼层规划方案代号（统一可通行性图 + 跨层单次 A\*） |
| **可通行性图 traversability graph** | 把点云化成「能走 / 不能走 + 代价」的图；楼梯 = 连接不同高度的渐变可通行边 |
| **LIO** | Lidar-Inertial Odometry，激光惯性里程计（FAST-LIO 系） |
| **sidecar** | 与主进程并跑、只干一件转发/桥接的小进程（如 `m20_lidar_sidecar.py`：rclpy → UDP 7777） |
| **overlay** | 叠在厂商/上游代码之上的自研补丁层（如 `drdds_overlay/`、dimos 的 M20 包） |
| **DDS / zenoh** | 机器人中间件。⚠️ 查 D1 厂商 ROS 栈**必带 zenoh 环境**，否则 `ros2 topic/service` 全空，极易误判「没起」 |
| **UWB** | 超宽带测距定位，D1 侧跟随用它跟人 |
| **ARC** | D1 自动回充对准流程。**不需要地图/定位**，排查时别往 fit%/重定位那边查 |
| **墓碑** | 文档里刻意保留的「被否方案 + 为什么否」记录（如 `d1_env.sh.example`）。防止后人「恢复」已被证伪的方案 |

---

## 6. 待补 / 疑问清单

- [ ] `dimostest/` 到底是什么？和 `dimosmain/` 什么关系（早期实验树？可以删？）
- [ ] `agibot-d1/t01/` 是哪个平台？和 `dimos-t01-venv/` 怎么配套？
- [ ] `localization1/`（home 下那个）是空的，只有 `.claude/` —— 是否已迁到 `GitHubs/localization1/`？
- [ ] `GitHubs/ascent` 是什么，为什么放进来？
- [ ] `icp_pose_bridge.py` 在整条定位链里的位置：是候选方案还是已弃？
- [ ] `2Dmap/` 里 6.24 / 7.02 / 无日期三套地图分别对应哪个现场？哪套是当前有效的？
- [ ] 五份 dimos 副本能否收敛成一份 + 分支？（先搞清各自的孤本改动，用 md5 对拍）

# 视觉检测学习

> 分册 **7 / 9** ｜ 上级 [学习文档](学习文档.md) ｜ 术语 [术语表](术语表.md)
> 前置：无（**可随时插入**） ｜ 最近更新 2026-09-07

---

## 0. 这册学什么

`recognize/` 下**三个独立的目标检测任务**：怎么分任务、数据从哪来、环境为什么脆弱。

学完应该能：说清为什么三个任务不能共用一个模型，以及 Blackwell 显卡上那个环境坑的判据。

> 这册和机器人主线**无依赖** —— 它是「狗身上的一个能力」，不是「狗能走路的前提」。

---

## 1. 三个任务（**不要混**）

| 目录 | 任务 | 视角 | 数据状态 |
|---|---|---|---|
| `ground/` | 地面垃圾识别 | 机器人第一视角 | TACO 冷启动 + D1 自采（**进行中**） |
| `pavement/` | 车行道路面病害（裂缝 / 龟裂 / 坑槽） | **行车记录仪** | jsai 2000 张，**可训** |
| `sidewalk/` | 人行道铺装病害（砖块破损 / 缺失） | 机器人第一视角 | 🔴 **无数据，找数据中** |

### 🔴 `pavement` 和 `sidewalk` 名字接近，**是两个任务**

| | `pavement/` | `sidewalk/` |
|---|---|---|
| 视角 | 车载（行车记录仪） | 机器人第一视角 |
| 对象 | 沥青马路裂缝 | 铺装砖破损 |
| 类别 | 裂缝 / 龟裂 / 坑槽 | 砖块破损 / 缺失 |
| 模型 | 不通用 | 不通用 |

**车道数据对砖块任务的迁移价值仅限于「什么是裂缝」这层纹理先验** —— 别指望拿 `pavement` 的模型直接上 `sidewalk`。

> 这是个反直觉点：两个任务在**表面语义**上都是「路面破损检测」，但视角、尺度、材质、类别定义全不同。
> **语义相近 ≠ 可迁移**，判断可迁移性要看视角和物理对象，不是看任务名字。

---

## 2. 环境（🔴 脆弱，别乱装）

已验证可用（**2026-08-14**）：

| | |
|---|---|
| GPU | **RTX 5070 Ti 16GB**（Blackwell, **sm_120**） |
| PyTorch | **2.11.0+cu128** |
| ultralytics | 8.4.120 |
| 基线模型 | **YOLO26-s**（`weights/yolo26s.pt`） |
| 实测速度 | **91 FPS @ imgsz=960**，未量化 |

```bash
source .venv/bin/activate     # 或直接用 .venv/bin/python
```

### 🔴 Blackwell 坑

- RTX 50 系必须用 **cu128 及以上**的 PyTorch 轮子
- **PyPI 默认源装出来的可能不含 sm_120 kernel**
- **判据**：训练时报 `no kernel image is available for execution on the device`
- 本项目 venv 已从 `download.pytorch.org/whl/cu128` 安装 ⇒ 🔴 **勿随意 `pip install torch` 覆盖**

> 这个坑的性质：`pip install torch` 会「成功」，import 也「成功」，只在**真正下发 kernel 时**才炸 ——
> 又一个「成功不代表可用」的例子（对照 [分册 4](定位与重定位学习.md) 的假收敛）。

### 目录约定

```
recognize/
├── .venv/                  共享虚拟环境
├── weights/                共享预训练权重（yolo26s.pt）
├── ground/
├── pavement/
└── sidewalk/
```

三任务**共享环境和预训练权重**，但**各自独立的数据和模型**。

---

## 3. 红线速查

| 红线 | 后果 |
|---|---|
| **`pavement` / `sidewalk` 不是一个任务** | 混用模型 = 结果全错 |
| **勿 `pip install torch` 覆盖 venv** | 丢 sm_120 kernel，训练时才炸 |
| **RTX 50 系必须 cu128+** | 同上 |

---

## 4. 动手清单

- [ ] 用 `.venv/bin/python` 跑一次 `torch.cuda.get_device_capability()`，确认返回 `(12, 0)`（sm_120）
- [ ] 跑一次 YOLO26-s 推理，验证 91 FPS @ imgsz=960 这个基线还成立
- [ ] 在 `pavement/`（有 2000 张 jsai 数据）上跑一次完整训练，走通全流程
- [ ] 把 `pavement` 训出的模型直接在 `sidewalk` 的样张上推理，**亲眼看到它不通用**
- [ ] 读 `ground/` 的 TACO 冷启动配置，搞清 D1 自采数据要怎么合进去
- [ ] 给 `sidewalk/` 找数据（当前最大瓶颈）：先明确类别定义和标注规范
- [ ] 读一遍 `recognize/README.md` 的目录约定，确认自己没在错误的目录下建实验

---

## 5. 自测问题

- [ ] 三个任务分别是什么？视角分别是什么？
- [ ] `pavement` 和 `sidewalk` 为什么模型不通用？列出四个维度的差别。
- [ ] 车道数据对砖块任务的迁移价值**只**剩哪一层？
- [ ] 从这个例子能提炼出什么关于「可迁移性判断」的教训？
- [ ] RTX 50 系（Blackwell, sm_120）为什么必须 cu128 以上的轮子？
- [ ] 装错了会报什么错？为什么这个错**只在训练时**才出现？
- [ ] 为什么这个坑和「假收敛」是同一类问题？
- [ ] 三个任务共享什么、各自独立什么？
- [ ] 当前三个任务各自的瓶颈是什么？

---

## 6. 笔记（日期倒序）

### 模板（复制这段往下写）

```markdown
### 2026-XX-XX ｜ <主题>
- **在学**：<项目/文件路径>
- **搞懂了**：<一句话结论，重点写「从代码里看不出来的」>
- **踩的坑**：<现象 → 真实原因 → 判据>
- **代码位置**：`path/to/file.py:123`
- **已回写到**：<哪份权威文档；没回写就写「待回写」>
- **仍不懂**：<搬到第 7 节>
```

<!-- 从这里往下写笔记 -->

---

## 7. 本册疑问

- [ ] `sidewalk/` 的数据到底从哪来？自采（D1 巡检时录）还是找公开集？
- [ ] `ground/` 的 D1 自采进行到哪一步了？标了多少张？
- [ ] 三个任务最终是部署到狗上实时跑，还是离线批处理巡检视频？
- [ ] 如果上狗实时跑，91 FPS 是在 5070 Ti 上测的 —— Orin NX 上还剩多少？需要量化吗？
- [ ] `recognize/reco.rar` 是什么（旧版打包？）
- [ ] 检测结果怎么和地图 / 位姿关联（病害要标在地图上才有用）？这是否需要接 [分册 4](定位与重定位学习.md)？
- [ ] YOLO26 选型的理由是什么？对比过别的吗？

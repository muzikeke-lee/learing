# 建图与点云学习

> 分册 **5 / 9** ｜ 上级 [学习文档](学习文档.md) ｜ 术语 [术语表](术语表.md)
> 前置：[激光雷达学习](激光雷达学习.md) ｜ 最近更新 2026-09-07

---

## 0. 这册学什么

**地图这个资产**：怎么建、怎么换、航点存在哪、点云怎么处理、怎么可视化。

学完应该能：说清一张地图牵连着哪些下游资产（航点、栅格图、巡线脚本），以及换图时哪些必须一起换。

> 核心认知：**地图不是一个文件，是一套坐标基准**。换地图 = 换掉所有下游资产的参照系。

---

## 1. 项目与文件

### 1.1 工具与代码

| 路径 | 是什么 | 要点 |
|---|---|---|
| `D1sidefellow/d1_docs/地图航点与webviz.md` | D1 换图 / 建图 / 录航点 / webviz 权威文档 | 🔴 **航点在 SQLite 不是 JSON**；**webviz 不读 DB** |
| `pcd_floor_split/split_floors.py` + `README.md` | 点云按楼层自动分层 | 见 2.3 |
| `agibot-d1/d1_switch_map.sh` `d1_reload_map_wp.sh` | 换图 / 重载地图+航点 | 两者分工要读清 |
| `dimosmain/tools/waypoint_recorder` | M20 航点录制 | 录之前先重定位 → [分册 4](定位与重定位学习.md) |
| `dimosmain/dimos/robot/deeprobotics/m20/waypoint_registry.py` | M20 航点管理 | — |
| `dimosmain/config/` | M20 的 waypoints + 地图 | — |
| `agibot-d1/map_fix_20260807/` | 某次地图修补 | 修的是什么？→ 第 7 节 |
| `agibot-d1/odin_map_view/` | 地图查看工具 | 待确认 |

### 1.2 地图数据资产

| 路径 | 内容 | 格式含义 |
|---|---|---|
| `2Dmap/` | `6.24map.*`、`7.02map.*`、`map.*` 三套 | `.pgm` 占据栅格图 + `.yaml` 元数据（分辨率/原点）+ `.zst` 压缩 |
| `2Dmap/7.02_dog_and_waypoint.png` `7.02map_view.png` | 带狗位姿和航点的可视化截图 | 对照用 |
| `3DMap/` | `full_cloud2.pcd` + 五个 `.rrd` | `.rrd` = **Rerun 录制**（`fc2_plan` / `fc2_stair` / `full_cloud111` / `full_cloud111_stair` / `full_cloud2_floors`） |
| `3DMap_NP-MFS/map_pcd/` | `full_cloud2.pcd`（**主测试地图**，三层，z ∈ [-11.9, 7.7]）、`full_cloud111.pcd` | → [分册 6](规划与导航学习.md) |
| `3DMap_NP-MFS/map_rrd/` | Rerun 录制 | — |
| `agibot-d1/D1_maps/` `D1_maps_0702/` | D1 建好的导航地图 | — |

> `.rrd` 文件名里的 `_stair` / `_floors` / `_plan` 说明它们是**不同阶段的可视化留档**，
> 按名字能反推出当时在调什么 —— 这本身是有用的历史线索。

---

## 2. 核心机制

### 2.1 🔴 航点存在 SQLite，不在 JSON

两条独立的坑：

1. **航点在 SQLite 数据库里**，不是 JSON 文件 ⇒ 想改航点不能编辑文本文件，要动 DB
2. **webviz 不读 DB** ⇒ webviz 上看到的航点**可能和实际执行的不一致**，不能拿 webviz 当真相

⇒ 判断「航点到底是什么」只能查 DB，界面和文件都不算证据。

### 2.2 🔴 录航点的前置条件

**必须先确认 fit ≥ 90%**，否则**静默偏 0.7m**（详见 [分册 4](定位与重定位学习.md)）。

录航点是「把当前定位结果固化下来」的动作 ⇒ 定位错了，航点就永久错了，而且**后面每次导航都按错的走**。
这是「定位层的静默错误如何污染下游资产」的最典型案例。

### 2.3 pcd_floor_split：楼层自动分层

**做什么**：沿 Z 轴用**高度直方图** + `scipy.find_peaks` 检出每块楼板 / 天花板，按相邻楼板把点云切成各楼层。

```python
from split_floors import split_pcd_to_floors
floors, slab_z = split_pcd_to_floors("building.pcd")
# floors[i] : 第 i 层的 (N,3) numpy 点数组
# slab_z    : 检出的楼板 z 坐标列表
```

依赖：`numpy` `open3d` `scipy`

**来源与改动**（算法蒸馏，不是原创）：

| | |
|---|---|
| 上游 | [Cloud2BIM](https://github.com/VaclavNezerka/Cloud2BIM)（MIT），点云→IFC/BIM 自动管线 |
| 原函数 | `aux_functions.py` 的 `identify_slabs` / `split_pointcloud_to_storeys` |
| 论文 | https://arxiv.org/html/2503.11498v2 |
| **我的改动 1** | 只保留「按楼层切分」核心，去掉凸包 / IFC 重建等重依赖 |
| **我的改动 2** | 把原版**固定阈值（0.6 × 峰值）**改成 `find_peaks` **相对突起度** |

**改动 2 为什么必要**：地面那个峰特别强，用「0.6 × 全局峰值」当阈值时，
楼上各层的楼板峰全都低于这个门槛 → **被地面强峰压制、检不出来**。
相对突起度（prominence）判断的是「这个峰比它周围高多少」，与全局最大值无关 ⇒ 不受地面峰影响。

> 这是个通用教训：**全局阈值在存在强主峰的分布上一定失效**，要换成局部/相对判据。

### 2.4 可视化：Rerun

`3DMap_NP-MFS/dimos-m20/.venv`：python 3.12 / **rerun 0.29.2** / **open3d 0.19.0** / **scipy 1.17**。

Rerun 支持 `--serve --host <ip>` 起服务后从浏览器看 ⇒ 可以在开发机跑、在别的机器上看。
`.rrd` 是它的录制格式，能存档回放。

---

## 3. 红线速查

| 红线 | 后果 |
|---|---|
| **航点在 SQLite 不在 JSON** | 改错地方 = 改了没生效 |
| **webviz 不读 DB** | 界面上的航点不是真相 |
| **fit < 90% 不准录航点** | 静默偏 0.7m，永久污染 |
| **换地图会废掉航点 / pgm / 巡线脚本** | 它们共享地图坐标基准 → [分册 4](定位与重定位学习.md) |
| **全局阈值在有强主峰时必失效** | 楼层检不出来（pcd_floor_split 的原版 bug） |

---

## 4. 动手清单

- [ ] 查一次 D1 的航点 SQLite DB，把表结构写进笔记；同时在 webviz 上看同一批航点，找出差异
- [ ] 跑 `split_pcd_to_floors()` 处理 `3DMap_NP-MFS/map_pcd/full_cloud2.pcd`，验证检出 3 层、`slab_z` 落在 z ∈ [-11.9, 7.7] 内
- [ ] 把检出的 `slab_z` 和 `2Dmap/` 里的栅格图对上，确认哪张 pgm 是哪一层
- [ ] 故意把 `find_peaks` 改回「0.6 × 峰值」固定阈值，复现「楼层被地面峰压制」的现象
- [ ] 打开 `2Dmap/*.yaml` 读懂 `resolution` / `origin` 字段，手算一个栅格坐标 ↔ 世界坐标的换算
- [ ] 用 Rerun 打开 `3DMap/full_cloud2_floors.rrd` 和 `fc2_stair.rrd`，反推当时在调什么
- [ ] 读清 `d1_switch_map.sh` 和 `d1_reload_map_wp.sh` 的分工

---

## 5. 自测问题

- [ ] D1 的航点存在哪？为什么 webviz 上看到的不能当真相？想确认航点只能怎么查？
- [ ] 录航点前为什么必须 fit ≥ 90%？错了的航点会造成什么长期后果？
- [ ] `.pgm` + `.yaml` 这一对里，`yaml` 提供了什么栅格图本身没有的信息？
- [ ] `pcd_floor_split` 的算法原理是什么（两步）？
- [ ] 它蒸馏自哪个开源项目？我去掉了什么、改了什么？
- [ ] 为什么把固定阈值（0.6 × 峰值）改成 `find_peaks` 相对突起度？不改会出现什么现象？
- [ ] 这个改动能推广成什么通用教训？
- [ ] 换一张地图，会连带废掉哪三类下游资产？为什么？
- [ ] `.rrd` 是什么格式？Rerun 怎么做到「开发机跑、别的机器看」？

---

## 6. 笔记（日期倒序）

### 模板（复制这段往下写）

```markdown
### 2026-XX-XX ｜ <主题>
- **在学**：<项目/文件路径>
- **搞懂了**：<一句话结论，重点写「从代码里看不出来的」>
- **踩的坑**：<现象 → 真实原因 → 判据>
- **代码位置**：`path/to/file.py:123`
- **已回写到**：<哪份权威文档；没回写就写「待回写」>
- **仍不懂**：<搬到第 7 节>
```

<!-- 从这里往下写笔记 -->

---

## 7. 本册疑问

- [ ] `2Dmap/` 里 6.24 / 7.02 / 无日期三套地图分别对应哪个现场？**哪套是当前有效的**？
- [ ] `D1_maps/` 和 `D1_maps_0702/` 的关系？新的替代旧的了吗？
- [ ] `map_fix_20260807/` 修的是什么问题？修完的图上线了吗？
- [ ] `agibot-d1/odin_map_view/` 是什么工具？和 webviz 什么关系？
- [ ] `full_cloud2.pcd` 和 `full_cloud111.pcd` 是同一栋楼的不同次采集，还是两栋楼？
- [ ] 建图流程本身（不是换图）在哪份文档里？是厂商建图还是自建？
- [ ] `pcd_floor_split` 在实际管线里用上了吗，还是只是实验？和 NP-MFS 的「不分层」思路冲突吗？

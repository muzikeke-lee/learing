# 激光雷达学习

> 分册 **3 / 9** ｜ 上级 [学习文档](学习文档.md) ｜ 术语 [术语表](术语表.md)
> 前置：[平台与部署学习](平台与部署学习.md) ｜ 最近更新 2026-09-07

---

## 0. 这册学什么

**传感器层**：雷达和 IMU 出什么数据、经过哪些进程、坐标系怎么对、外参谁负责标。
再加上另外两种测距传感器（超声 USS、RTK）。

学完应该能：从雷达出点到急停触发，把整条数据链的每一跳（进程 / topic / 端口）说出来。

> 这册是 [定位与重定位](定位与重定位学习.md) 和 [建图与点云](建图与点云学习.md) 的共同前置 ——
> **坐标系和单位搞错，后面两册全是白干**。

---

## 1. 项目与文件

| 路径 | 是什么 | 坑在哪 |
|---|---|---|
| `D1sidefellow/d1_docs/硬件与传感器.md` | D1 传感器权威文档：关节温度 / 电池 / 雷达外参 / 回充 | **雷达外参不用自己标**；「关节温度全 0」是 overlay 装错不是电机问题 |
| `dimosmain/bridge/lidar_imu_bridge/` | C++ ROS 节点：`rt/LIDAR/POINTS` → `rt/lidar_relayed` | — |
| `dimosmain/runtime/m20_lidar_sidecar.py` | rclpy 订阅 `/lidar_relayed` → UDP 7777 | 需要 rclpy，所以不能做成 dimos module |
| `dimosmain/dimos/robot/deeprobotics/m20/safety_monitor.py` | 雷达近障急停，UDP 收 sidecar | 链路任一跳断 = **静默失去避障** |
| `D1sidefellow/D1-FAST-LIO/README.md` | 自建 LIO 方案研究（**零代码**） | IMU 单位和坐标轴两个致命坑，见 2.3 |
| `D1sidefellow/RADARA22test/README.md` | **超声（USS）唯一权威**，进展也记在那 | — |
| `D1sidefellow/D1RTK/README.md` + `agibot-d1/RTK现场测试说明.md` | RTK | — |
| `~/中狗传感器数据获取说明.docx` | 中狗 ZSM-1 的传感器取数方式 | — |
| `agibot-d1/智元四足机器人D1 Max SDK开发指南V0.0.6.pdf` | 厂商 SDK 指南（传感器接口在这） | — |

### 两台狗的雷达对照

| | **M20** | **D1 Max** |
|---|---|---|
| 雷达 | **Livox Mid-360** | frame_id `rslidar_head`（**型号待确认**） |
| 取数路径 | ROS topic → 自研 bridge → sidecar → UDP | 厂商 ROS 栈（**查询必带 zenoh 环境**） |
| IMU | 随雷达（Mid-360 内置） | **三路 IMU**，acc 单位是 **g** |
| 外参 | 待确认 | **不用自己标**（厂商已给） |
| 超声 | 待确认 | 有，见 `RADARA22test/` |

---

## 2. 核心机制

### 2.1 M20 避障数据链（四跳，全链路都要会查）

```
雷达硬件
  │  ROS topic  rt/LIDAR/POINTS
  ▼
bridge/lidar_imu_bridge/          ← C++ ROS 节点，跑在 106
  │  ROS topic  rt/lidar_relayed
  ▼
runtime/m20_lidar_sidecar.py      ← rclpy，独立进程
  │  UDP 7777
  ▼
m20/safety_monitor.py             ← dimos module，近障急停
```

**为什么要这么多跳**：中间那两跳是运行时隔离的代价 —— dimos 主进程不能直接用 rclpy，
所以必须靠一个 rclpy 进程把 ROS 世界的数据翻译成 UDP 喂进来。

⚠️ **这条链是静默失效的**：任何一跳挂掉，狗照样能走，只是**没有避障了**。所以起栈后必须验证末端
（`safety_monitor` 真的在收到点）而不是只看首端（雷达有没有出点）。

### 2.2 🔴 玻璃：雷达 + 地图双盲区

- **雷达盲**：激光穿透玻璃，看不到障碍
- **地图也盲**：建图时同样穿透，玻璃不会出现在地图里

⇒ 两道防线同时失效。**唯一兜底是人工**：地图上手动标障碍 + 现场人盯。
🔴 **玻璃区禁止自主导航。**

### 2.3 🔴 D1 自建 LIO 的两个致命坑

`D1-FAST-LIO` 是**方案研究、零代码**，但坑已经踩明白了：

| 坑 | 后果 | 正确做法 |
|---|---|---|
| 三路 IMU 的 acc 单位是 **g**，不是 m/s² | 加速度差 9.8 倍，滤波器直接崩 | 读数先 ×9.81 |
| IMU 与点云 **`frame_id` 同名**（都是 `rslidar_head`）**但坐标轴差 90°** | 填单位阵**必发散** | 必须填真实旋转，不能因为同名就当同坐标系 |

第二个坑尤其阴 —— `frame_id` 相同是最强的「它们是同一个坐标系」信号，而这里恰好相反。

### 2.4 关节温度全 0 的真实原因

现象：读关节温度全是 0，很容易判成「电机坏了 / 传感器没接」。
真实原因：**overlay 装错**。⇒ 先查 overlay 再查硬件。

### 2.5 回充链路（ARC）—— 不要往定位那边查

🔴 **ARC 对准不需要地图 / 定位**，排查时别往 fit% / 重定位那条线查（详见 [分册 4](定位与重定位学习.md)）。

| 现象 | 真实原因 | 处理 |
|---|---|---|
| 遥控器上「去充电」**不出现** | 桩没 discovered | **先等** —— 链路本就断续，平均 1.8 分钟自己回来 |
| 等了还不出现，且 RK 上 `dmesg` 有 `no role for macid` | **狗这边**的 AP 服务挂了 | 在 RK 上重启 `hostapd`。⚠️ **给桩断电重启没用**，坏的不是桩 |
| 「去充电」按得下去但**狗不动** | 遥控器握权时 RK 吃掉 ARC 速度指令 | **先关遥控器**。判据：`errors` 某一位不变 |

⇒ 三个现象、三个完全不同的根因，靠**判据**区分而不是靠猜。相关 SOP 见 `~/机器狗搭载充电桩使用的SOP及固件包资料/`。

---

## 3. 红线速查

| 红线 | 为什么 |
|---|---|
| **玻璃区禁止自主导航** | 雷达 + 地图双盲，无自动兜底 |
| **雷达外参不用自己标**（D1） | 厂商已给，自己标是白费且引入误差 |
| **IMU acc 要按 g 处理**（D1） | 差 9.8 倍 |
| **IMU / 点云 frame_id 同名但差 90°** | 填单位阵必发散 |
| **起栈后必须验证避障链末端** | 链路静默失效，狗照走 |
| **查厂商 ROS 栈必带 zenoh 环境** | 否则 topic 全空，误判「没起」 |
| **ARC 问题不要查定位** | 完全不相关的两条链 |

---

## 4. 动手清单

- [ ] 在 M20 上把避障链四跳逐跳查一遍：ROS topic 有没有数 → sidecar 有没有转发 → UDP 7777 有没有包 → `safety_monitor` 有没有触发
- [ ] 故意停掉 sidecar，观察狗**照样能走但没有避障**，把这个现象记进笔记（⚠️ 找空旷场地、慢速、有人盯）
- [ ] 读 D1 三路 IMU 的原始读数，验证静止时模长 ≈ 1（而不是 ≈ 9.81），确认单位是 g
- [ ] 把 IMU 和点云的坐标轴画出来，标出那 90° 差在哪个轴上
- [ ] 用 `tools/udp_test` 和 `state_monitor` 各查一次传感器状态
- [ ] 读 `RADARA22test/README.md`，写清超声当前能用到什么程度

---

## 5. 自测问题

- [ ] M20 避障数据链完整四跳是什么（进程 / topic / 端口全说出来）？
- [ ] 中间为什么必须有 `m20_lidar_sidecar.py` 这一跳，不能让 `safety_monitor` 直接订 ROS topic？
- [ ] 这条链断了狗会有什么表现？为什么「看雷达有出点」不足以证明避障可用？
- [ ] 为什么**玻璃是雷达 + 地图双盲区**？工程上怎么兜底？
- [ ] D1 三路 IMU 的 acc 单位是什么？填错会怎样？
- [ ] IMU 和点云 `frame_id` 同名（`rslidar_head`）却差 90° —— 为什么这个坑特别容易踩？填单位阵会怎样？
- [ ] D1 的雷达外参要不要自己标？为什么？
- [ ] 「关节温度全 0」的真实原因是什么？排查顺序应该是什么？
- [ ] 「去充电」不出现 / 按得下去但狗不动 —— 两个现象的根因分别是什么？各自的判据是什么？
- [ ] 为什么给充电桩断电重启是无效操作？

---

## 6. 笔记（日期倒序）

### 模板（复制这段往下写）

```markdown
### 2026-XX-XX ｜ <主题>
- **在学**：<项目/文件路径>
- **搞懂了**：<一句话结论，重点写「从代码里看不出来的」>
- **踩的坑**：<现象 → 真实原因 → 判据>
- **代码位置**：`path/to/file.py:123`
- **已回写到**：<哪份权威文档；没回写就写「待回写」>
- **仍不懂**：<搬到第 7 节>
```

<!-- 从这里往下写笔记 -->

---

## 7. 本册疑问

- [ ] D1 的雷达具体型号是什么？（`rslidar_head` 只是 frame 名）
- [ ] M20 的 Mid-360 外参谁标的、在哪个文件里？
- [ ] D1 三路 IMU 分别装在哪、各自用途是什么？自建 LIO 该用哪一路？
- [ ] 玻璃盲区除了人工标注，有没有别的传感器能补（超声？视觉？）—— 这是 `RADARA22test` 想解决的问题吗？
- [ ] M20 有没有超声？如果没有，近障只靠雷达的风险有多大？
- [ ] `lidar_imu_bridge` 除了转发还做了什么（去畸变？时间同步？）
- [ ] RTK 在现在的定位方案里是候选还是已上线？和 fit% 那套怎么融合？

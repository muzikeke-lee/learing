# 定位与重定位学习

> 分册 **4 / 9** ｜ 上级 [学习文档](学习文档.md) ｜ 术语 [术语表](术语表.md)
> 前置：[激光雷达学习](激光雷达学习.md) ｜ 最近更新 2026-09-07

---

## 0. 这册学什么

**最容易踩坑、也最值得多花时间的一层。** 定位错了，上面的规划、航点、巡线全是错的，而且**大多是静默的**。

学完应该能：不靠猜、只靠判据，判断当前定位是「真收敛 / 假收敛 / 丢失 / 没起」中的哪一种。

🔴 这一册的核心认知：**接口返回成功 ≠ 定位正确**。这层的所有 API 都会骗人。

---

## 1. 项目与文件

| 路径 | 是什么 | 地位 |
|---|---|---|
| `D1sidefellow/d1_docs/定位与校正器.md` | D1 定位 / 重定位 / 校正器 / fit% | 🔴 **必读，本册第一优先** |
| `D1sidefellow/D1-FAST-LIO/README.md` | 自建 LIO 方案研究（FAST-LIO 系 + 全局校正 + 重定位） | **零代码**，纯方案 |
| `dimosmain/README.md`「关键机制」 | M20 的 UDP 位姿读取与重定位协议 | — |
| `dimosmain/tools/relocalize` | M20 重定位诊断工具 | — |
| `agibot-d1/d1_reloc.sh` `d1_loc_watchdog.sh` `d1_loc_recover.sh` | 本机侧运维：重定位 / 看门狗 / 恢复 | 三个脚本的分工要读清 |
| `GitHubs/lidar_localization_ros2` | 外部：激光定位 ROS2 实现 | 参考 / 候选 |
| `GitHubs/localization1/icp_localization_ros2` + `icp_pose_bridge.py` | 外部 ICP 定位 + **我自己写的 pose 桥接** | 候选还是已弃？→ 第 7 节 |
| `GitHubs/ORB_SLAM3` | 外部：视觉 SLAM | 参考 |
| `datasets/euroc/` | EuRoC 数据集 | 离线验证用 |

### 两台狗的定位方案对照

| | **M20** | **D1 Max** |
|---|---|---|
| 定位来源 | 厂商定位，UDP 读位姿 | 厂商定位 + **校正器** |
| 置信度指标 | 待确认 | **fit%** |
| 重定位方式 | UDP `Type=2101 Cmd=1` | `/load_map_service` + 校正器 |
| 自研程度 | 只读、不改 | 有校正器；自建 LIO 在研（零代码） |

---

## 2. 核心机制

### 2.1 M20：UDP 读位姿与重定位

| 用途 | 协议 | 字段 |
|---|---|---|
| 读导航位姿 | UDP `Type=1007 Cmd=2` | `(PosX, PosY, Yaw)`，**地图系** |
| 重定位 | UDP `Type=2101 Cmd=1` | `{PosX, PosY, PosZ, Yaw}` |

**Yaw 约定**（已现场校验，偏差 2°）：**弧度**；**0 = +x = 东**；**逆时针为正**。

> 这三条约定每一条都可能反 —— 单位、零点、旋向。**现场校验过**这件事本身是最有价值的信息，
> 换平台时必须重新校验，不能沿用。

🔴 **重定位是开机 / 丢失后的必做动作**，是录航点和导航的**前提**。跳过它后面全错。

### 2.2 D1：fit% 与「四种形态」

**fit%** = 定位置信度。

🔴 **录航点前必须确认 fit ≥ 90%**，否则**静默偏 0.7m** —— 航点录下来是错的，而且当场看不出来。

定位「死活」有 **4 种形态**，而**常规的 3 条判据会全体假阴性**（判成「活着」但其实没活）。
⇒ 具体四种形态和正确判据在 `定位与校正器.md`，**这是本册最需要精读的一段**。

### 2.3 🔴 假收敛：`success=True` 不可信

`/load_map_service` 会返回 `success=True` **却静默忽略**这次请求。

- 现象：接口说成功，日志没报错
- 实际：地图没加载 / 定位没收敛，位姿是错的
- ⇒ **不能用返回值判断成功**，必须验证末端状态（fit% 等）

这是本册的中心教训：**这一层的每个「成功」都要用独立判据复核。**

### 2.4 自建 LIO：方案研究的三条结论

`D1-FAST-LIO` **零代码**，但已经得出三条硬结论：

| 结论 | 为什么 |
|---|---|
| 三路 IMU 的 acc 单位是 **g** 不是 m/s² | 差 9.8 倍，滤波必崩 → [分册 3](激光雷达学习.md) |
| IMU 与点云 `frame_id` **同名但坐标轴差 90°** | 填单位阵**必发散** → [分册 3](激光雷达学习.md) |
| 🔴 **必须拿厂商 `map.pcd` 当先验图** | **自己建图 = 航点 / pgm / 巡线脚本全废** |

第三条是最重要的架构约束：地图不只是定位的输入，它还是**航点、栅格图、巡线脚本共享的坐标基准**。
换掉地图 = 换掉所有下游资产的坐标系。

---

## 3. 红线速查

| 红线 | 后果 |
|---|---|
| **`success=True` 不能信** | 假收敛，位姿静默错误 |
| **fit < 90% 不准录航点** | 静默偏 0.7m |
| **开机 / 丢失后必做重定位** | 航点、导航全错 |
| **常规 3 条「定位活着」判据会全体假阴性** | 判成活着但其实没活 |
| **自建 LIO 必须用厂商 `map.pcd` 当先验** | 否则航点 / pgm / 巡线脚本全废 |
| **Yaw 约定要现场校验**（弧度 / 0=东 / 逆时针正） | 单位、零点、旋向任一反 = 走反方向 |
| **ARC 回充问题不属于这一层** | 别往 fit% / 重定位查 → [分册 3](激光雷达学习.md) |

---

## 4. 动手清单

- [ ] 精读 `定位与校正器.md`，把「4 种形态 × 判据」做成一张表贴进笔记
- [ ] 在 D1 上把 4 种形态各复现 / 观察一次，验证常规 3 条判据如何假阴性
- [ ] 故意在 fit < 90% 时录一个航点，量出实际偏差，亲眼确认「静默」这个性质
- [ ] 调一次 `/load_map_service`，构造出 `success=True` 但没生效的情况
- [ ] 在 M20 上用 `tools/relocalize` 重定位，再用 `Type=1007 Cmd=2` 读回位姿，验证 Yaw 的三条约定
- [ ] 读清 `d1_reloc.sh` / `d1_loc_watchdog.sh` / `d1_loc_recover.sh` 三者分工，各写一句话
- [ ] 静止读 D1 三路 IMU，确认 acc 模长 ≈ 1（g）

---

## 5. 自测问题

- [ ] 为什么 `/load_map_service` 返回 `success=True` **也不能信**？什么叫假收敛？
- [ ] 定位「死活」的 4 种形态是哪四种？常规 3 条判据为什么会全体假阴性？正确判据是什么？
- [ ] 录航点前为什么必须确认 fit ≥ 90%？不确认会静默偏多少？为什么当场看不出来？
- [ ] M20 读导航位姿和重定位分别用什么 UDP 协议？字段是什么？
- [ ] M20 的 Yaw：单位、零点、旋向分别是什么？为什么「现场校验过」这件事很重要？
- [ ] 重定位在什么时候必做？跳过会怎样？
- [ ] 自建 LIO 为什么**必须**拿厂商 `map.pcd` 当先验图？自己建图会废掉哪三类东西？
- [ ] 这一册的中心教训用一句话说是什么？

---

## 6. 笔记（日期倒序）

### 模板（复制这段往下写）

```markdown
### 2026-XX-XX ｜ <主题>
- **在学**：<项目/文件路径>
- **搞懂了**：<一句话结论，重点写「从代码里看不出来的」>
- **踩的坑**：<现象 → 真实原因 → 判据>
- **代码位置**：`path/to/file.py:123`
- **已回写到**：<哪份权威文档；没回写就写「待回写」>
- **仍不懂**：<搬到第 7 节>
```

<!-- 从这里往下写笔记 -->

---

## 7. 本册疑问

- [ ] `icp_pose_bridge.py` 在整条定位链里的位置：候选方案还是已弃？为什么写了它？
- [ ] `GitHubs/` 四个定位仓库（lidar_localization_ros2 / icp_localization_ros2 / ORB_SLAM3 / ascent）各自评估到哪一步了？结论是什么？
- [ ] M20 有没有类似 fit% 的置信度指标？如果没有，怎么判定位死活？
- [ ] 校正器（D1）具体在校正什么？和重定位的分工是什么？
- [ ] `datasets/euroc/` 是给哪个方案做离线验证的？跑过没有、结果如何？
- [ ] 自建 LIO 这条线现在的结论是「继续做」还是「搁置」？触发条件是什么？
- [ ] RTK 和 fit% 那套怎么融合？→ 也记在 [分册 3](激光雷达学习.md) 第 7 节
